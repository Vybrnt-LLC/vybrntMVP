import 'package:flutter/material.dart';

class PostDetailBlocProvider extends StatelessWidget {
  final String postID;
  final bool isOrg;

  const PostDetailBlocProvider({Key key, this.postID, this.isOrg})
      : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
